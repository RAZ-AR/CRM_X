# CRM X — Norion

Next.js. Данные пока в браузере; общая база — Aiven Postgres (см. `DEPLOY.md`).

```bash
cd web && npm install && npm run dev
```

Логины: `1111` Owner, `2222` Armen, `3333` Маркетолог (пароль = логин).
